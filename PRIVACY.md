# Privacy and Local Data

AI Desk Phone 是本地优先的实验性项目，但启用语音、模型、搜索或在线地图后并非完全离线。本文件说明项目代码可确认的数据流和清理边界；第三方服务自己的保留期限与处理规则以其服务条款和隐私政策为准。

## 本地保存的数据

| 位置 | 内容 | 默认保留行为 | 清理影响 |
| --- | --- | --- | --- |
| `.env` | 语音和 Ark API Key、服务配置 | 一直保留，除非用户删除或覆盖 | 删除后需要重新配置在线语音和模型能力 |
| `data/recordings/` | 用户录音 WAV | 不自动过期 | 删除后无法重放或重新识别这些录音 |
| `data/tts/` | 非流式 TTS 回退生成的 WAV | 不自动过期 | 只影响已生成语音文件 |
| `data/agent_sessions/` | Agent 会话 JSONL | 不自动过期 | 控制台“删除会话”只删除当前会话文件；清理目录会删除全部历史会话 |
| `logs/` 与根目录 `*.log` | 启动、硬件和错误日志 | 不自动过期 | 会失去对应排障证据 |
| `config/ai_desk_phone_console.json` | 非密钥控制台配置 | 一直保留 | 下次启动回到默认配置或重新生成 |

这些敏感目录已由 `.gitignore` 排除，但“未被 Git 跟踪”不等于已经加密。使用共享电脑时应依赖 Windows 账户隔离和磁盘加密，并在提交 Issue 前脱敏。

## 发送到外部的数据

| 功能 | 可能发送的数据 | 目的地 |
| --- | --- | --- |
| ASR | 听筒录制的音频、识别参数和热词配置 | `.env` 中配置的豆包/火山引擎语音端点 |
| TTS | Agent 回复文本、音色和合成参数 | `.env` 中配置的豆包/火山引擎语音端点 |
| 角色模型与润色 | 用户文本、识别结果、Agent 上下文和系统提示词 | `ARK_CHAT_COMPLETIONS_ENDPOINT` |
| 网页搜索 | 用户明确要求搜索时提取的查询词 | Bing 搜索；系统浏览器还会直接访问打开的结果页 |
| 地球指挥中心 | IP 地址、浏览器请求信息、地图范围及图层请求 | OpenFreeMap、Esri、EOX/Sentinel-2、NASA GIBS 和 unpkg |

项目代码没有通用使用统计或广告跟踪逻辑。网络服务仍可能按各自规则记录请求元数据。不要把密钥、密码、身份信息或不必要的完整会话放入搜索词或模型提示词。

## ESP32 上的数据

Wi-Fi SSID、密码和命令主机保存在 ESP32 NVS 的 `ailandline` namespace。网页“删除 Agent 会话”不会清除设备凭据。转让、回收或离开可信网络前，应执行整片擦除，然后按需重新烧录：

```powershell
.\.venv\Scripts\platformio.exe run `
  -d firmware\esp32c3_gpio0_21_test `
  -e esp32s3_gpio0_21_test `
  -t erase `
  --upload-port COM7
```

整片擦除会清除固件和 NVS；先确认串口和目标板正确。重新使用设备时必须重新烧录并配网。

## 清理与报告

清理前先停止控制台，确认路径位于当前仓库，并只删除不再需要的具体文件或子目录。项目当前没有自动保留期限和一键清除全部数据功能。

公开提交日志、截图或录音前，至少移除 API Key、Wi-Fi 凭据、个人路径、局域网地址、设备标识、完整对话和可识别的声音。安全问题按 `SECURITY.md` 私下报告。
