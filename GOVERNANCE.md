# Project Governance and Release Policy

本文说明 AI Desk Phone 的长期分支、版本和发布规则。项目当前由仓库所有者 `Damue01` 维护。

## 长期分支

仓库只保留两条长期分支：

| 分支 | 职责 | 更新方式 |
| --- | --- | --- |
| `hg113-main` | 默认分支和当前开发基线；文档、CI 与安全修复以此为准 | 通过经过审查和验证的提交更新 |
| `main` | 发布分支；保存最近一次准备对外发布的状态 | 仅在发布检查完成后从 `hg113-main` 更新 |

`main` 可以暂时落后于 `hg113-main`，但不在 `main` 上单独开发。紧急修复先进入 `hg113-main`，验证后再发布到 `main`。临时开发分支在合并或放弃后删除；不再保留长期的 `feature/*`、`codex/*`、`legacy/*` 或自动依赖更新分支。

## 版本规则

- Git 标签使用 `vMAJOR.MINOR.PATCH`，遵循语义化版本的主版本、次版本和补丁版本结构。
- 首个稳定版本以前使用 `v0.x.y`；次版本可能包含不兼容变化，必须在 `CHANGELOG.md` 中明确说明。
- 修复兼容问题使用补丁版本；新增向后兼容能力使用次版本；稳定版之后的不兼容变化使用主版本。
- 尚未进入标签的用户可见改动统一记录在 `CHANGELOG.md` 的 `Unreleased` 下。
- 截至 2026-07-17，项目尚未发布首个稳定版本；当前支持范围仍是 `hg113-main` 的最新提交。

## 发布检查

发布维护者按以下顺序操作：

1. 确认工作区无密钥、Wi-Fi 凭据、录音、会话、日志和个人路径。
2. 在 Windows 上运行全部 Python 测试，并编译 ESP32-S3 主固件环境。
3. 验证模拟器启动；涉及硬件的版本还要记录实机验证结果。
4. 更新 README、参考手册、API 参考、BOM、隐私说明和 `CHANGELOG.md`。
5. 按 `docs/DEPENDENCIES.md` 生成当次发布的解析后依赖清单，并复核第三方许可证。
6. 把 `Unreleased` 内容归入具体版本和日期。
7. 将通过检查的 `hg113-main` 更新到 `main`，在 `main` 对应提交创建签名或受保护的版本标签和 GitHub Release。

版本发布后如发现高风险安全问题，按 `SECURITY.md` 协调修复与披露。
