# AI Desk Phone 威胁模型

本文描述当前实验性原型的信任边界，不表示项目已经过安全审计。

## 1. 保护目标

- Windows 主机和用户文件；
- `.env` 中的 Volcengine、Doubao、Ark 等凭据；
- 录音、转写、TTS 文件和 Agent 会话；
- ESP32-S3 配网凭据和设备控制；
- 用户的物理安全、低压硬件和电话外壳；
- 第三方服务账号与费用。

## 2. 信任边界

| 区域 | 默认信任 | 说明 |
| --- | --- | --- |
| Windows 主机与当前用户 | 高 | 控制台、文件、音频和命令均在此运行 |
| 本仓库代码和已审查依赖 | 中高 | 更新或插件仍需审查 |
| ESP32-S3 固件 | 中 | 可发送未认证遥测并接收命令 |
| 家庭/工作室可信局域网 | 中 | 协议无加密和设备身份认证 |
| 浏览器中的本地控制台 | 中 | HTTP API 无登录，依赖回环绑定 |
| Agent 模型输出 | 低 | 可能受提示注入或生成错误命令 |
| 外部网页和搜索结果 | 低 | 内容不可信，不能直接转为高权限动作 |
| Volcengine、Doubao、Ark、地图服务 | 外部 | 数据处理受各提供商条款约束 |
| 公网或访客 Wi-Fi | 不信任 | 不应暴露任何控制端口 |

## 3. 攻击面与防护

### 3.1 HTTP 控制台

风险：API 可修改 `.env`、控制硬件、提交 Agent 任务、清理内存和启动固件烧录，且没有认证。

当前防护：

- 启动脚本默认绑定 `127.0.0.1`；
- API 拒绝 `Sec-Fetch-Site: cross-site` 和不匹配当前 `Host` 的 `Origin`；
- POST 强制使用 `application/json`，跨站简单表单不能触发写操作；
- 外部 Lucide/MapLibre 脚本固定版本并校验 SHA-384 SRI；
- 文档禁止公网暴露。

剩余风险：这些浏览器约束不是身份认证；同一 Windows 用户下的恶意进程、同源脚本或已获得本机执行能力的攻击者仍可调用 API。未来若支持远程访问，必须先加入身份认证、随机 CSRF token、权限分级和 TLS 终止方案。

### 3.2 Agent 与 shell

风险：模型、语音转写或网页内容可能诱导 Agent 执行不安全命令。

当前防护：

- 默认 `confirm_sensitive` 不允许 shell；
- shell 只在 `commander`、`developer`、`trusted` 开放；
- 有危险模式黑名单、15 秒超时和输出截断；
- 项目文件工具限制在仓库根目录且默认只读；
- skill 只从项目 `.pi/skills/` 加载。

剩余风险：黑名单不是沙箱，也无法覆盖所有危险命令。高权限档位只适合用户明确要求、主机已备份且仓库可信的场景。

### 3.3 局域网与 ESP32

风险：同网段攻击者可以伪造 UDP 遥测、发送硬件命令、连接 TCP 8768 或观察配网流量。

当前防护：

- 建议隔离在可信家庭/工作室网络；
- HTTP 与设备命令端口不做公网转发；
- 硬件命令等待状态回报确认。

剩余风险：设备没有证书、共享密钥、签名或重放保护。未来可增加设备配对密钥、消息认证码和网络 allowlist。

### 3.4 Wi-Fi 配网

风险：固定 AP 密码和 HTTP 页面会在配网窗口暴露 SSID/密码。

当前防护：配网只在无凭据或多次失败后启动，并在保存后退出；凭据写入 ESP32 NVS。

操作要求：只在现场短时配网，完成后确认热点关闭；不要在公共场所或不可信人员附近进入配网模式。

### 3.5 密钥和本地数据

风险：`.env`、`data/`、`logs/` 可能包含密钥、录音、文本、设备地址或任务结果。

当前防护：这些路径被 `.gitignore` 排除；网页密钥字段应避免回显完整值。

操作要求：

- 提交前运行 `git status` 和敏感信息检查；
- 分享日志前脱敏；
- 不需要时删除录音和 session；
- 怀疑泄露时立即在提供商侧轮换密钥。

### 3.6 第三方服务

启用 ASR、TTS、Ark、天气、搜索、地图或实时影像后，相关数据会发送到对应服务。项目无法控制第三方的保留策略、地域、可用性或计费。用户必须自行阅读提供商条款并确认数据适合上传。

### 3.7 电气和物理风险

风险：电话外线可能包含高压振铃信号；错误供电、裸露焊点或大电流铃器可能损坏设备或造成人身伤害。

当前防护：项目明确只使用低压结构，要求电话外线断开、LED 限流、音频模块独立供电，并为大功率负载增加驱动。

不在范围：接入公共电话网、市电、未知原板电源或超出参考电压的改造。

## 4. 安全默认值

- HTTP：`127.0.0.1:8765`
- Agent 权限：`confirm_sensitive`
- 模拟器：`--simulation-only --no-serial --no-actions`，运行时硬性禁用硬件 I/O、固件烧录和 Windows 快捷键
- 实机启动：关闭自动模拟
- 密钥：只在被忽略的 `.env`
- Wi-Fi 凭据：ESP32 NVS 或被忽略的 `wifi_credentials.h`

## 5. 已知限制

- HTTP 和设备协议没有身份认证；
- 配网 AP 使用固定密码；
- shell 拦截依赖模式匹配，不是操作系统沙箱；
- 第三方 CDN 和地图服务属于运行时供应链；
- 固件没有签名更新或安全启动要求；
- 项目尚未进行独立渗透测试和电气认证。

## 6. 评审触发条件

以下改动必须同步更新本文：

- 新增远程访问、账号、令牌或公网部署；
- 改变 Agent 权限、shell、文件写入或网络工具；
- 新增模型提供商、数据持久化或遥测；
- 改变 ESP32 配网、固件更新或命令认证；
- 改变供电、音频、铃器或电话线连接方式。
