# Security Policy

## 受支持版本

项目尚未发布稳定版本。安全修复只面向默认分支 `hg113-main` 的最新提交；历史分支、个人 fork、旧固件和修改后的接线不在支持范围内。

## 私下报告漏洞

请优先使用 GitHub 的[私密安全公告入口](https://github.com/Damue01/AIDeskPhone/security/advisories/new)。报告中请包含：

- 受影响的提交、文件或固件环境；
- 可复现步骤和影响；
- 是否需要真实硬件、局域网或第三方凭据；
- 已脱敏的日志、请求或最小示例；
- 你建议的修复方向（可选）。

如果私密安全公告不可用，请通过[维护者 GitHub 主页](https://github.com/Damue01)建立私下联系，不要在公开 Issue 中披露利用细节、密钥、录音或个人数据。

维护者会尽力确认报告、评估范围并协调披露时间，但实验性项目目前不承诺固定响应 SLA。

## 安全范围

适合私下报告的示例：

- 默认配置下可从非本机访问未授权的高风险 HTTP 操作；
- 绕过 Agent 命令权限或危险命令拦截；
- 路径遍历、任意文件读写或命令注入；
- `.env`、录音、会话或密钥意外泄露；
- 恶意设备遥测触发桌面端代码执行或持久化；
- 固件配网、命令协议或更新流程中的可利用缺陷。

通常不作为漏洞处理：

- 主动使用 `--host 0.0.0.0` 后把无认证 API 暴露到不受信任网络；
- 在 `commander`、`developer` 或 `trusted` 档位明确允许的 shell 执行；
- 固定配网热点密码、明文局域网协议等已记录的实验性限制；
- 需要物理拆机并连接到已声明低压调试引脚的行为；
- 第三方云服务自身的可用性、计费或账号权限问题。

即使不属于漏洞，也欢迎提交加固建议。完整边界见 [THREAT_MODEL.md](THREAT_MODEL.md)。

## 安全使用要求

- 不连接电话外线或未知高压线路。
- HTTP 保持绑定 `127.0.0.1`；不做公网端口转发。
- ESP32 和电脑只接入可信局域网。
- 默认使用 `confirm_sensitive` Agent 权限。
- 定期轮换密钥，提交前检查 `.env`、日志、录音和 Git diff。

